spring:
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:prod}
server:
  port: 8080
instagram:
  baseurl: https://graph.instagram.com/
  userid: ${INSTAGRAM_USERID}
  accesstoken: ${INSTAGRAM_ACCESSTOKEN}
slack:
  token: ${SLACK_TOKEN}
gcp:
  baseurl: http://metadata.google.internal/computeMetadata/v1/project/attributes/